Contents moved here to keep the project root clean:

- Phase 3  WDM sping 25 - ECOM-1.docx — course rubric / reference (not used by the app). Not tracked by git (.gitignore); keep a copy locally if you need it.
- dist/ — last Vite build output (regenerate with: npm run build from project root).
- .DS_Store.* — macOS folder metadata (safe to delete).
